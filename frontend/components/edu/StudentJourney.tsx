"use client";

import Image from "next/image";
import Link from "next/link";
import { AnimatePresence, motion, useReducedMotion } from "framer-motion";
import { useMemo, useState } from "react";
import { ArrowLeft, ArrowRight, Check, ChevronDown, FileText, Globe, Graduation, Heart, MapPin, Search, Shield, Spark, User, Wallet } from "./Icons";

type Stage = "auth" | "otp" | "profile" | "matches" | "documents" | "complete";
type University = { id: number; code: string; university: string; course: string; city: string; country: string; fee: number; duration: string; intake: string; base: number; scholarship: string; rank: string; reasons: string[] };

const universities: University[] = [
  { id: 1, code: "RMIT", university: "RMIT University", course: "Master of Data Science", city: "Melbourne", country: "Australia", fee: 28.4, duration: "2 years", intake: "Feb 2027", base: 96, scholarship: "₹6.5L available", rank: "Top 125 globally", reasons: ["Academics exceed entry criteria", "Within your tuition range", "Preferred city match"] },
  { id: 2, code: "UQ", university: "University of Queensland", course: "Master of Business Analytics", city: "Brisbane", country: "Australia", fee: 31.2, duration: "2 years", intake: "Feb 2027", base: 92, scholarship: "₹8L available", rank: "World top 50", reasons: ["Strong academic alignment", "English score qualified", "High employability outcomes"] },
  { id: 3, code: "MON", university: "Monash University", course: "Master of Applied Data Science", city: "Melbourne", country: "Australia", fee: 33.8, duration: "2 years", intake: "Jul 2027", base: 89, scholarship: "₹5L available", rank: "World top 40", reasons: ["Course preference match", "Preferred destination", "Scholarship eligible"] },
  { id: 4, code: "UCD", university: "University College Dublin", course: "MSc Business Analytics", city: "Dublin", country: "Ireland", fee: 24.6, duration: "1 year", intake: "Sep 2027", base: 86, scholarship: "₹3L available", rank: "Top 200 globally", reasons: ["Excellent budget fit", "Accelerated program", "English score qualified"] },
  { id: 5, code: "UBC", university: "University of British Columbia", course: "Master of Data Science", city: "Vancouver", country: "Canada", fee: 37.5, duration: "10 months", intake: "Sep 2027", base: 83, scholarship: "Merit aid available", rank: "World top 40", reasons: ["High course relevance", "Strong academic fit", "Career-focused curriculum"] },
];

const docs = [
  ["Passport", "Identity and visa processing", "ready"], ["Academic transcripts", "10th, 12th and undergraduate records", "ready"], ["Degree certificate", "Proof of completed qualification", "ready"], ["English scorecard", "IELTS / PTE / TOEFL / Duolingo", "ready"], ["Statement of purpose", "Your academic goals and motivation", "action"], ["Letters of recommendation", "Two academic or professional references", "action"], ["Financial evidence", "Tuition and living expense capacity", "action"], ["Updated CV", "Academic and work experience", "ready"],
] as const;

export function StudentJourney({ kiosk = false }: { kiosk?: boolean }) {
  const reduceMotion = useReducedMotion();
  const [stage, setStage] = useState<Stage>("auth");
  const [authMode, setAuthMode] = useState<"register" | "login">("register");
  const [otp, setOtp] = useState("");
  const [name, setName] = useState("Aarav Sharma");
  const [country, setCountry] = useState("Australia");
  const [city, setCity] = useState("Melbourne");
  const [course, setCourse] = useState("Data Science");
  const [budget, setBudget] = useState(35);
  const [test, setTest] = useState("IELTS");
  const [score, setScore] = useState("7.5");
  const [search, setSearch] = useState("");
  const [saved, setSaved] = useState<number[]>([1, 2, 3]);
  const [sort, setSort] = useState("Best match");
  const [toast, setToast] = useState("");

  const matches = useMemo(() => universities
    .filter((u) => (!country || u.country === country) && `${u.university} ${u.course} ${u.city}`.toLowerCase().includes(search.toLowerCase()))
    .map((u) => ({ ...u, score: Math.max(72, u.base - (budget < u.fee ? 5 : 0) + (city === u.city ? 2 : 0)) }))
    .sort((a, b) => sort === "Lowest tuition" ? a.fee - b.fee : b.score - a.score), [budget, city, country, search, sort]);

  const progress = stage === "auth" || stage === "otp" ? 1 : stage === "profile" ? 2 : stage === "matches" ? 3 : 4;
  const transition = reduceMotion ? { duration: 0 } : { duration: 0.28, ease: "easeOut" as const };
  const go = (next: Stage) => { setStage(next); window.scrollTo({ top: 0, behavior: reduceMotion ? "auto" : "smooth" }); };
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(""), 2800); };
  const toggleSave = (id: number) => { setSaved((s) => s.includes(id) ? s.filter((x) => x !== id) : [...s, id]); notify(saved.includes(id) ? "Removed from shortlist" : "Added to your shortlist"); };

  return (
    <main className={`journey-app ${kiosk ? "kiosk-mode" : ""}`}>
      <a className="skip-link" href="#journey-content">Skip to content</a>
      <header className="app-header"><Link href="/" aria-label="Cybrik Solutions home"><Image src="/cybrik-logo.png" alt="Cybrik Solutions" width={620} height={180} priority /></Link><div className="step-track" aria-label={`Step ${progress} of 4`}><span>Step {progress} of 4</span><i><b style={{ width: `${progress * 25}%` }} /></i></div><div className="app-help"><span className="secure"><Shield size={16} /> Secure session</span><Link href="/">Exit</Link></div></header>
      <div className="app-steps" aria-hidden="true">{["Account", "Your profile", "Explore matches", "Document plan"].map((s, i) => <div className={i + 1 <= progress ? "active" : ""} key={s}><b>{i + 1 < progress ? <Check size={15} /> : i + 1}</b><span>{s}</span></div>)}</div>
      <AnimatePresence mode="wait">
        <motion.div id="journey-content" key={stage} initial={reduceMotion ? false : { opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} exit={reduceMotion ? { opacity: 1 } : { opacity: 0, y: -8 }} transition={transition}>
          {stage === "auth" && <AuthScreen mode={authMode} setMode={setAuthMode} name={name} setName={setName} onNext={() => go("otp")} />}
          {stage === "otp" && <OtpScreen otp={otp} setOtp={setOtp} onBack={() => go("auth")} onNext={() => go("profile")} />}
          {stage === "profile" && <ProfileScreen name={name} country={country} setCountry={setCountry} city={city} setCity={setCity} course={course} setCourse={setCourse} budget={budget} setBudget={setBudget} test={test} setTest={setTest} score={score} setScore={setScore} onNext={() => go("matches")} />}
          {stage === "matches" && <MatchesScreen matches={matches} saved={saved} search={search} setSearch={setSearch} sort={sort} setSort={setSort} toggleSave={toggleSave} onEdit={() => go("profile")} onNext={() => go("documents")} />}
          {stage === "documents" && <DocumentsScreen saved={saved} toggleSave={toggleSave} onBack={() => go("matches")} onComplete={() => go("complete")} />}
          {stage === "complete" && <CompleteScreen name={name} saved={saved.length} onDone={() => { setStage("auth"); setOtp(""); }} />}
        </motion.div>
      </AnimatePresence>
      <AnimatePresence>{toast && <motion.div className="toast" role="status" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }}><Check size={18} />{toast}</motion.div>}</AnimatePresence>
    </main>
  );
}

function AuthScreen({ mode, setMode, name, setName, onNext }: { mode: "register" | "login"; setMode: (v: "register" | "login") => void; name: string; setName: (v: string) => void; onNext: () => void }) {
  return <section className="auth-layout"><div className="auth-story"><div className="auth-art"><Globe size={190} /><div className="route-line r1"/><div className="route-line r2"/><span className="pin p1">AU</span><span className="pin p2">UK</span><span className="pin p3">CA</span></div><span className="kicker">Your study journey starts here</span><h1>One profile.<br />A world of <span>possibility.</span></h1><p>Discover programs that fit your ambitions, eligibility, and budget—with every next step organized.</p><div className="auth-proof"><div><strong>2,400+</strong><span>Programs</span></div><div><strong>18</strong><span>Countries</span></div><div><strong>94%</strong><span>Strong matches</span></div></div></div><div className="auth-form-wrap"><form className="form-card" onSubmit={(e) => { e.preventDefault(); onNext(); }}><div className="form-tabs"><button type="button" className={mode === "register" ? "active" : ""} onClick={() => setMode("register")}>Create account</button><button type="button" className={mode === "login" ? "active" : ""} onClick={() => setMode("login")}>Sign in</button></div><h2>{mode === "register" ? "Let’s get to know you" : "Welcome back"}</h2><p>{mode === "register" ? "Your details help us personalize your experience." : "Continue building your global education plan."}</p>{mode === "register" && <Field label="Full name"><User /><input required autoComplete="name" value={name} onChange={(e) => setName(e.target.value)} /></Field>}<Field label="Mobile number"><span className="prefix">+91</span><input required type="tel" autoComplete="tel" defaultValue="98765 43210" /></Field>{mode === "register" && <Field label="Email address"><span className="at">@</span><input required type="email" autoComplete="email" defaultValue="aarav@example.com" /></Field>}<button className="button button-full button-lg" type="submit">{mode === "register" ? "Create my account" : "Send login code"}<ArrowRight /></button><small className="form-note"><Shield size={15} /> We’ll verify your mobile with a secure one-time code.</small></form></div></section>;
}

function OtpScreen({ otp, setOtp, onBack, onNext }: { otp: string; setOtp: (v: string) => void; onBack: () => void; onNext: () => void }) {
  return <section className="center-screen"><button className="back-link" onClick={onBack}><ArrowLeft /> Back</button><div className="otp-card"><div className="success-icon"><Shield /></div><span className="kicker">Secure verification</span><h1>Check your phone</h1><p>We sent a 6-digit code to <strong>+91 98765 43210</strong></p><label className="otp-input"><span>One-time code</span><input autoFocus inputMode="numeric" maxLength={6} value={otp} onChange={(e) => setOtp(e.target.value.replace(/\D/g, ""))} placeholder="• • • • • •" /></label><button className="button button-full button-lg" disabled={otp.length !== 6} onClick={onNext}>Verify and continue <ArrowRight /></button><button className="resend" onClick={() => setOtp("123456")}>Use demo code <strong>123456</strong></button></div></section>;
}

function ProfileScreen({ name, country, setCountry, city, setCity, course, setCourse, budget, setBudget, test, setTest, score, setScore, onNext }: { name: string; country: string; setCountry: (v:string)=>void; city:string; setCity:(v:string)=>void; course:string; setCourse:(v:string)=>void; budget:number; setBudget:(v:number)=>void; test:string; setTest:(v:string)=>void; score:string; setScore:(v:string)=>void; onNext:()=>void }) {
  return <section className="profile-page"><div className="page-intro"><span className="kicker">Hello, {name.split(" ")[0]}</span><h1>Build your student profile</h1><p>A few details help us remove ineligible programs and rank the options that genuinely fit.</p></div><form className="profile-grid" onSubmit={(e) => { e.preventDefault(); onNext(); }}><ProfileSection icon={<Graduation />} title="Academic background" note="Your most recent qualifications"><div className="input-grid"><Select label="Highest qualification" defaultValue="Bachelor’s degree" options={["Bachelor’s degree","Master’s degree","Diploma"]}/><FieldPlain label="Degree / specialization"><input defaultValue="B.Tech Computer Science" /></FieldPlain><Select label="10th score" defaultValue="88%" options={["88%","80%","75%"]}/><Select label="12th score" defaultValue="84%" options={["84%","80%","75%"]}/><Select label="12th board" defaultValue="CBSE" options={["CBSE","ICSE","State Board"]}/><Select label="UG score" defaultValue="8.2 CGPA" options={["8.2 CGPA","7.5 CGPA","70%"]}/></div></ProfileSection><ProfileSection icon={<Globe />} title="Study preferences" note="Where and what you’d like to study"><div className="input-grid"><Select label="Preferred country" value={country} onChange={setCountry} options={["Australia","Canada","Ireland"]}/><Select label="Preferred city" value={city} onChange={setCity} options={country === "Australia" ? ["Melbourne","Sydney","Brisbane"] : country === "Canada" ? ["Vancouver","Toronto"] : ["Dublin"]}/><FieldPlain label="Preferred course"><input value={course} onChange={(e) => setCourse(e.target.value)} /></FieldPlain><FieldPlain label="Preferred university (optional)"><input placeholder="Any university" /></FieldPlain></div></ProfileSection><ProfileSection icon={<Spark />} title="English proficiency" note="Your latest valid test score"><div className="input-grid two"><Select label="Test type" value={test} onChange={setTest} options={["IELTS","PTE","TOEFL","Duolingo"]}/><FieldPlain label={`${test} score`}><input value={score} onChange={(e) => setScore(e.target.value)} inputMode="decimal" /></FieldPlain></div></ProfileSection><ProfileSection icon={<Wallet />} title="Tuition budget" note="Your preferred annual tuition range"><div className="budget-control"><div><span>₹5 lakhs</span><strong>Up to ₹{budget} lakhs / year</strong><span>₹60 lakhs</span></div><input aria-label="Maximum annual tuition budget" type="range" min="5" max="60" value={budget} onChange={(e) => setBudget(Number(e.target.value))} /></div></ProfileSection><div className="profile-actions"><span><Shield size={17}/> Your profile auto-saves on this device</span><button className="button button-lg" type="submit">Show my matches <ArrowRight /></button></div></form></section>;
}

function MatchesScreen({ matches, saved, search, setSearch, sort, setSort, toggleSave, onEdit, onNext }: { matches: (University & {score:number})[]; saved:number[]; search:string; setSearch:(v:string)=>void; sort:string; setSort:(v:string)=>void; toggleSave:(id:number)=>void; onEdit:()=>void; onNext:()=>void }) {
  return <section className="matches-page"><aside className="filter-panel"><div><span className="kicker">Your preferences</span><h2>Shape your results</h2></div><button className="edit-profile" onClick={onEdit}>Edit full profile <ArrowRight size={17}/></button><FilterSummary icon={<Graduation/>} label="Academics" value="B.Tech · 8.2 CGPA"/><FilterSummary icon={<Globe/>} label="Destination" value="Australia · Melbourne"/><FilterSummary icon={<Spark/>} label="English" value="IELTS · 7.5"/><FilterSummary icon={<Wallet/>} label="Tuition" value="Up to ₹35L / year"/><div className="filter-tip"><Spark/><strong>Tip</strong><p>Widening your city preference could reveal 8 more eligible programs.</p></div></aside><div className="results-panel"><div className="results-head"><div><span className="kicker">Personalized for you</span><h1>{matches.length} eligible programs found</h1><p>Sorted by how well each course fits your profile and preferences.</p></div><div className="shortlist-count"><Heart/><strong>{saved.length}</strong><span>shortlisted</span></div></div><div className="result-tools"><label className="search-box"><Search/><input value={search} onChange={(e)=>setSearch(e.target.value)} placeholder="Search university, course, or city"/></label><label className="sort-box"><span>Sort</span><select value={sort} onChange={(e)=>setSort(e.target.value)}><option>Best match</option><option>Lowest tuition</option></select><ChevronDown/></label></div><div className="result-list">{matches.map((u, index)=><article className="result-card" key={u.id}><div className="result-rank">#{index+1}</div><div className={`uni-mark large ${["blue","green","amber"][index%3]}`}>{u.code}</div><div className="result-main"><div className="result-title"><div><span>{u.rank}</span><h3>{u.university}</h3><p>{u.course}</p></div><div className="match-score"><strong>{u.score}%</strong><span>profile match</span></div></div><div className="result-meta"><span><MapPin/>{u.city}, {u.country}</span><span><Wallet/>₹{u.fee}L / year</span><span><Graduation/>{u.duration}</span><span><Globe/>{u.intake}</span></div><div className="why-fit"><strong>Why this fits</strong>{u.reasons.map(r=><span key={r}><Check/>{r}</span>)}</div><div className="result-bottom"><span className="scholarship"><Spark/>{u.scholarship}</span><div><button className={`save-button ${saved.includes(u.id)?"saved":""}`} onClick={()=>toggleSave(u.id)} aria-label={`${saved.includes(u.id)?"Remove":"Add"} ${u.university} ${saved.includes(u.id)?"from":"to"} shortlist`}><Heart/>{saved.includes(u.id)?"Shortlisted":"Shortlist"}</button><button className="button button-sm" onClick={()=>toggleSave(u.id)}>View course <ArrowRight size={16}/></button></div></div></div></article>)}{matches.length===0&&<div className="empty-state"><Search size={32}/><h3>No exact matches found</h3><p>Try a broader search or update your destination preferences.</p><button className="button button-sm" onClick={()=>setSearch("")}>Clear search</button></div>}</div><div className="sticky-next"><div><strong>{saved.length} program{saved.length===1?"":"s"} shortlisted</strong><span>Ready to build your personalized document plan?</span></div><button className="button button-lg" disabled={!saved.length} onClick={onNext}>Continue to documents <ArrowRight/></button></div></div></section>;
}

function DocumentsScreen({ saved, onBack, onComplete }: { saved:number[]; toggleSave:(id:number)=>void; onBack:()=>void; onComplete:()=>void }) {
  const selected = universities.filter(u=>saved.includes(u.id)); const ready=docs.filter(d=>d[2]==="ready").length;
  return <section className="documents-page"><button className="back-link" onClick={onBack}><ArrowLeft/> Back to matches</button><div className="documents-head"><div><span className="kicker">Your personalized plan</span><h1>Everything you’ll need,<br/>organized in one place.</h1><p>Built from your {selected.length} shortlisted programs and destination requirements.</p></div><div className="readiness"><div className="readiness-ring"><strong>{Math.round((ready/docs.length)*100)}%</strong><span>ready</span></div><div><strong>{ready} of {docs.length} documents</strong><span>You’re making great progress</span></div></div></div><div className="document-layout"><div><div className="section-bar"><h2>Admission documents</h2><span>{docs.length} items</span></div><div className="document-list">{docs.map(([title,description,status])=><article key={title} className="document-row"><div className={`doc-icon ${status}`}><FileText/></div><div><h3>{title}</h3><p>{description}</p></div><span className={`doc-status ${status}`}>{status==="ready"?<><Check/>Ready</>:"Action needed"}</span><button onClick={()=>{}}>{status==="ready"?"View":"Start"}<ArrowRight/></button></article>)}</div></div><aside className="plan-summary"><span className="kicker">Plan summary</span><h2>Your shortlisted routes</h2>{selected.map(u=><div className="route-summary" key={u.id}><div className="uni-mark blue">{u.code}</div><div><strong>{u.university}</strong><span>{u.course}</span><small>{u.city} · {u.intake}</small></div></div>)}<hr/><div className="visa-note"><Shield/><div><strong>Visa requirements included</strong><p>Australia-specific financial, health, and identity documents are part of this plan.</p></div></div><button className="button button-full button-lg" onClick={onComplete}>Save and complete <ArrowRight/></button><small className="plan-note">This demo stores your choices only for the current session.</small></aside></div></section>;
}

function CompleteScreen({ name, saved, onDone }: { name:string; saved:number; onDone:()=>void }) {
  return <section className="complete-screen"><div className="complete-card"><div className="complete-check"><Check size={34}/></div><span className="kicker">Journey saved</span><h1>You have a plan, {name.split(" ")[0]}.</h1><p>Your {saved} shortlisted programs and personalized document checklist are ready for your next step.</p><div className="complete-stats"><div><strong>{saved}</strong><span>Programs saved</span></div><div><strong>{docs.length}</strong><span>Documents planned</span></div><div><strong>Feb ’27</strong><span>Target intake</span></div></div><div className="complete-note"><Spark/><div><strong>What happens next?</strong><p>A Cybrik advisor can review your shortlist and help turn your document plan into applications.</p></div></div><Link href="/" className="button button-full button-lg" onClick={onDone}>Return to Cybrik home <ArrowRight/></Link><button className="text-button" onClick={onDone}>Start another journey</button></div></section>;
}

function Field({ label, children }: { label:string; children:React.ReactNode }) { return <label className="field-wrap"><span>{label}</span><div className="field-shell">{children}</div></label>; }
function FieldPlain({ label, children }: { label:string; children:React.ReactNode }) { return <label className="plain-field"><span>{label}</span>{children}</label>; }
function Select({ label, options, value, onChange, defaultValue }: { label:string; options:string[]; value?:string; onChange?:(v:string)=>void; defaultValue?:string }) { return <label className="plain-field"><span>{label}</span><div className="select-wrap"><select value={value} defaultValue={value===undefined?defaultValue:undefined} onChange={(e)=>onChange?.(e.target.value)}>{options.map(o=><option key={o}>{o}</option>)}</select><ChevronDown/></div></label>; }
function ProfileSection({ icon, title, note, children }: { icon:React.ReactNode; title:string; note:string; children:React.ReactNode }) { return <fieldset className="profile-section"><legend><span>{icon}</span><span><strong>{title}</strong><small>{note}</small></span></legend>{children}</fieldset>; }
function FilterSummary({ icon,label,value }:{icon:React.ReactNode;label:string;value:string}){return <div className="filter-summary"><span>{icon}</span><div><small>{label}</small><strong>{value}</strong></div><ChevronDown/></div>}
